# Production overrides: use with base compose.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
services:
  dealer-dashboard:
    environment:
      NODE_ENV: production
    restart: unless-stopped
